# DistributedMonitoringNetwork
Configure machines to be named manager or workers inside /etc/hosts. One manager machine and every other machine should be assigned as worker machines in numerical order.
